//created a file for the favorites function.

import React from "react";

function Favorites() {
    return (
        <div>
            <h1>This is a test message for the favorites page</h1>
        </div>
    );
};

export default Favorites;  